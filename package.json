{
  "name": "ts-axios",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Promise XMLHttpRequest",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "parcel ./src/index.html",
    "test": "npm run lint && jest start",
    "lint": "tslint --format stylish --project tsconfig.json",
    "build": "parcel build ./src/index.ts"
  },
  "devDependencies": {
  }
}
